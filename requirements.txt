# Core (rule engine, calibration maths, reports)
numpy>=1.26

# Perception (Phase 1 & 2 pipelines)
ultralytics>=8.3        # YOLO detection + pose + ByteTrack/BoT-SORT
opencv-python>=4.9      # video IO, calibration UI, team colours

# Dev
pytest>=8.0
